# System-level runtime (not installable via pip)
# Install with your distro's package manager:
#
#   Fedora/RHEL:   sudo dnf install python3-gobject gtk4 libadwaita gtksourceview5
#   Debian/Ubuntu: sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-4.0 \
#                                   gir1.2-adw-1 gir1.2-gtksource-5
#   Arch:          sudo pacman -S python-gobject gtk4 libadwaita gtksourceview5

# No pip-installable runtime deps.
# For development tooling only:
# ruff
# mypy
# pygobject-stubs
